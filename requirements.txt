# Core (needed everywhere, dev machine or Pi)
# NOTE: opencv-contrib-python, not plain opencv-python, and pinned <5.
# OpenCV 5.0 (released 2026) moved CascadeClassifier and cv2.face out of
# the main package entirely -- both are required here (CascadeClassifier
# for face tracking in vision.py, cv2.face for LBPH guest recognition).
# Installing opencv-contrib-python gives you both the main-package API
# and these extras in a single package; do not also install opencv-python
# alongside it.
opencv-contrib-python>=4.9,<5
numpy>=1.26

# Raspberry Pi 4 hardware backends
picamera2>=0.3
gpiozero>=2.0
pigpio>=1.78
sounddevice>=0.4
simpleaudio>=1.0
# Pinned: playsound 1.3.0's packaging is broken under modern setuptools
# (fails to build with "OSError: could not get source code" on many
# Python versions, including 3.14). 1.2.2 is the last version with
# working prebuilt wheels.
playsound==1.2.2
pyttsx3>=2.90
